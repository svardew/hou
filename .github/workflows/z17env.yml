name: env ref
on:
  workflow_dispatch:
  push:
    branches: ['**']


jobs:
  exoe:
    runs-on: ubuntu-latest
    env:
      Foo: pages
    steps:
      - name: echox
        run: |
          echo tgtt

        